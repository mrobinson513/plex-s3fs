apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plex-s3fs.fullname" . }}-test-connection"
  labels:
    {{- include "plex-s3fs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plex-s3fs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
